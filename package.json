{
  "name": "simple-text-editor",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "ELECTRON_ENABLE_LOGGING=1 NODE_ENV=development electron .",
    "build": "electron-builder -l portable",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^13.0.1",
    "electron-builder": "^22.10.5",
    "electron-reloader": "^1.2.1"
  }
}
